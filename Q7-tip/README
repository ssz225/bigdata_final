Within the Q7-tip directory:

MapReduce codes are matched in pairs of m% / r% files.
Using the fares dataset s3://fares-data-2010/:
	mmed, rmed
	m0, r0
	mn0, rn0
	mct, rct
Using the 2010 trip_fare_join dataset s3://trip-fare-join/2010/:
	m, r
	mdate, rdate
	
AWS EMR output stored in q7-output directory.

To plot, run plot.py from the package pack.